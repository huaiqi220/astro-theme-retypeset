# 克隆仓库
git clone <仓库地址>

# 进入项目目录
cd <仓库名称>

# 全局安装 pnpm（如果未安装）
npm install -g pnpm

# 安装依赖
pnpm install

# 启动开发服务器
pnpm dev

开发指令：

pnpm dev


部署指令：

npx astro build && npx wrangler dev

npx astro build && npx wrangler deploy


R2图像存储面板

https://dash.cloudflare.com/